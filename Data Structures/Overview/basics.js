class Basic {
    //Blueprint to create an object 
    //Same as java - no main method, you can have properties 
    //I hate the term - syntactical sugar 
    //Fuck that 
    //Instance methods - push, pop, methods which work on the individual instance of the class. So different objects will have different outputs based on the attributes of the object
    //Class methods are static methods - they only exist on the class itself and will affect all of them 

    constructor(a, b) {
        this.a = a;
        this.b = b;
    }





}